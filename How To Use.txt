Download the folder/git clone it:
in terminal paste the given command:
pip install -r requirements.txt
or simply install numpy and sympy using pip
rest instructions can be found in respective notebooks
